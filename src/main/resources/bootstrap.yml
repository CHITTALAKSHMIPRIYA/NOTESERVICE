spring:
  cloud:
    config:
      discovery:
        service-id: configserver
      uri: http://localhost:8888
          
        
management:
  endpoints:
    web:
      exposure:
        include: '*'
          